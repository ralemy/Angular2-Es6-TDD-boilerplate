div(*ngIf="hero")
    h2 {{hero.name}} details!
    div
        label id:
        |    {{hero.id}}
    div
        label name:
        input.heroName([(ngModel)]="hero.name" type="text" placeholder="name" )

